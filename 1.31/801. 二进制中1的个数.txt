#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n;
    cin >> n;
    while(n--){
        int x,s=0;
        cin >> x;
        for(int i=x;i;i -= i&-i) s++; //i&-i是i的二进制表示最低位的值
        cout << s <<" ";
    }
    return 0;
}