#include<bits/stdc++.h>
using namespace std;

int a[1010][1010];
int rec[1010][1010];
int main()
{
    int n,m,q;
    cin >> n >> m >> q;
    for(int i=1;i<=n;i++){
        for(int j=1;j<=m;j++){
            cin >> a[i][j];
            rec[i][j] = rec[i-1][j]+rec[i][j-1]-rec[i-1][j-1]+a[i][j];
        }
    }
    // for(int i=0;i<=n;i++){
    //     for(int j=0;j<=m;j++){
    //         cout << rec[i][j] << " ";
    //     }
    //     cout << endl;
    // }
    //rec[i-1][j]+rec[i][j-1]-rec[i-1][j-1]+a[i][j]
    while(q--){
        int x1,x2,y1,y2;
        cin >> x1 >> y1 >> x2 >> y2;
        cout << rec[x2][y2] -rec[x2][y1-1] - rec[x1-1][y2] + rec[x1-1][y1-1] << endl;
    }
}